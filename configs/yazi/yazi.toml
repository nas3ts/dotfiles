[opener]
glow = [
    { run = "glow -p %s", block = true, desc = "Glow", for = "unix" },
]
edit = [
    { run = "$EDITOR %s", block = true, for = "unix" },
]

[open]
prepend_rules = [
    { url = "*.md", use = ["glow", "edit", "reveal"] },
]