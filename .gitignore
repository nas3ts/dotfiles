# Secrets (never commit)
.env

# Personal config (manual editing required).
# Note: gitignore only affects untracked files. If a file listed here is
# already tracked, run `git rm --cached <path>` to stop tracking it; the
# entry then prevents accidental re-adding.
.aliae/env.yml

# Yazi plugins (installed by ya pkg install)
configs/yazi/plugins/

# Test/utility files in mpv (not part of config)
configs/mpv/test*.py
configs/mpv/updatefiles.bat
configs/mpv/update.sh

# Templated by install.sh (per-user paths)

